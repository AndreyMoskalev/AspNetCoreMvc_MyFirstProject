const AuthorizationOpen = () => {
    document.getElementById('authorization-box').classList.add('open');
}

const AuthorizationClose = () => {
    document.getElementById('authorization-box').classList.remove('open');
}